---
import Layout from '../layouts/Layout.astro'
import RecipeCard from '../components/RecipeCard.astro'

const allDiners = await Astro.glob('./diner/*.md')
---

<Layout title="Diner">
  <h1>🍽️ Diner</h1>
  <p class="size-20">Seizoensgebonden en vegetarische maaltijden, divers en lekker.</p>
  <ul class="recipes">
    {allDiners.map((diner) => (
      <RecipeCard href={diner.url}>
        {diner.frontmatter.title}
      </RecipeCard>
    ))}
  </ul>
</Layout>
