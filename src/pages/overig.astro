---
import Layout from '../layouts/Layout.astro'
import RecipeCard from '../components/RecipeCard.astro'

const allOthers = Object.values(import.meta.glob('./overig/*.md', { eager: true }))
---

<Layout title="Overig">
  <h1>🍨 Overig</h1>
  <p class="size-20">Dressings, desserts, drankjes, kuidenmixjes, en al het andere.</p>
  <ul class="recipes">
    {allOthers.map((other) => (
      <RecipeCard href={other.url}>
        {other.frontmatter.title}
      </RecipeCard>
    ))}
  </ul>
</Layout>
